// Projects — the list of everything the signed-in person can see.

import { ProjectsView } from "@/components/projects/projects-view";

export const metadata = { title: "Projects — Tielora" };

export default function ProjectsPage() {
  return <ProjectsView />;
}
